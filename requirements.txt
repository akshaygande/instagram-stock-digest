# Python dependencies for instagram-stock-digest
# Note: torch is installed separately in CI with CPU-only flag to save space.
# See .github/workflows/daily_digest.yml for the install order.

instaloader>=4.13
openai-whisper>=20231117
google-genai>=1.0
requests>=2.31
